// Hằng số dùng chung cho phần PWA (manifest, meta theme-color, service worker).

/** Màu thanh trạng thái khi chạy dạng app — khớp `--primary` trong globals.css. */
export const THEME_COLOR = "#2e871c";

/** Màu nền splash screen lúc khởi động — khớp `--background`. */
export const BACKGROUND_COLOR = "#ffffff";

/** Trang dự phòng khi mất mạng (service worker precache sẵn). */
export const OFFLINE_URL = "/offline";
